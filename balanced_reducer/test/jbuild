(library
 ((name balanced_reducer_test)
  (libraries (balanced_reducer
              base
              expect_test_helpers_kernel))
  (preprocess (pps (ppx_jane ppxlib.runner)))))

(jbuild_version 1)
